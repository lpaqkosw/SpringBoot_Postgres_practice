import {DRAWER_OPEN} from './action';

const initialState={
    drawerOpen: false
};

function reducer(state=initialState,action){
    switch(action.type){
        case DRAWER_OPEN: return{
            
        }
    }
}
